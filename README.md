# บอทเพลง Discord (สไตล์ Murphy)

บอทเพลงสำหรับ Discord ที่ได้รับแรงบันดาลใจจาก **Murphy uwu** สร้างโดย Manus AI บอทนี้ใช้ Slash Commands และมีระบบห้องควบคุมเพลงโดยเฉพาะ ทำให้การฟังเพลงในเซิร์ฟเวอร์ของคุณง่ายและสนุกยิ่งขึ้น

## ✨ คุณสมบัติเด่น

- **ระบบห้องควบคุมเพลง:** ใช้คำสั่ง `/setup` เพื่อสร้างห้องสำหรับขอเพลงโดยเฉพาะ
- **ใช้งานง่าย:** แค่พิมพ์ชื่อเพลงหรือ URL ในห้องควบคุม บอทก็จะเล่นให้ทันที (ไม่ต้องใช้ prefix!)
- **ปุ่มควบคุม Interactive:** ควบคุมเพลงได้ง่ายๆ ผ่านปุ่ม (Play/Pause, Skip, Stop, Loop, Volume, Queue และอื่นๆ)
- **Slash Commands:** ทันสมัยและใช้งานง่าย
- **รองรับหลายแพลตฟอร์ม:** เล่นเพลงจาก YouTube, SoundCloud และอื่นๆ
- **บันทึกข้อมูล:** บอทจะจำห้องควบคุมเพลงของแต่ละเซิร์ฟเวอร์ไว้ แม้จะปิด-เปิดใหม่

## 📋 สิ่งที่ต้องมี

1.  **Node.js v16.9.0 หรือสูงกว่า**
2.  **บัญชี Discord และเซิร์ฟเวอร์ที่คุณมีสิทธิ์ Admin**
3.  **Discord Bot Token และ Client ID**
    - สร้างบอทได้ที่ [Discord Developer Portal](https://discord.com/developers/applications)

## ⚙️ การติดตั้งและตั้งค่า

1.  **Clone a project หรือดาวน์โหลดไฟล์โค้ดทั้งหมด**

2.  **ติดตั้ง Dependencies:**
    ```bash
    npm install
    ```

3.  **สร้างไฟล์ `.env`:**
    สร้างไฟล์ใหม่ชื่อ `.env` ในโฟลเดอร์หลักของโปรเจกต์ แล้วใส่ Token ของคุณ

    ```
    DISCORD_TOKEN=ใส่โทเค็นบอทของคุณที่นี่
    ```

4.  **แก้ไขไฟล์ `config.js` (สำคัญมาก):**
    เพื่อให้บอทอ่าน Token จากไฟล์ `.env` อย่างปลอดภัย คุณต้องแก้ไขไฟล์ `config.js`

    หาบรรทัด:
    ```javascript
    token: process.env.DISCORD_TOKEN || 'YOUR_OLD_TOKEN',
    ```

    แก้ไขเป็น:
    ```javascript
    token: process.env.DISCORD_TOKEN,
    ```
    **คำเตือน:** อย่าลบ `clientId` ออกจากไฟล์ `config.js` เพราะจำเป็นสำหรับการลงทะเบียน Slash Commands

5.  **รันบอท:**
    ```bash
    npm start
    ```
    เมื่อรันครั้งแรก บอทจะทำการลงทะเบียน Slash Commands โดยอัตโนมัติ อาจใช้เวลาสักครู่เพื่อให้คำสั่งปรากฏบน Discord

## 🚀 การใช้งาน

1.  **เชิญบอทเข้าเซิร์ฟเวอร์ของคุณ** โดยให้สิทธิ์ `Administrator` หรือสิทธิ์ที่จำเป็น (Send Messages, Manage Channels, Connect, Speak, etc.)

2.  **ใช้คำสั่ง `/setup`:**
    - พิมพ์ `/setup` ในช่องแชทไหนก็ได้
    - คุณสามารถกำหนดชื่อห้องได้ เช่น `/setup music`
    - บอทจะสร้างห้องควบคุมเพลงขึ้นมาใหม่ พร้อมข้อความแนะนำและปุ่มควบคุม

3.  **เล่นเพลง:**
    - เข้าไปอยู่ในช่องเสียง (Voice Channel)
    - ไปที่ห้องควบคุมเพลงที่สร้างขึ้น
    - **พิมพ์ชื่อเพลงหรือ URL** ที่ต้องการลงในห้องนั้นได้เลย!
    - บอทจะค้นหาและเริ่มเล่นเพลง พร้อมอัปเดตข้อความควบคุม

4.  **ควบคุมเพลง:**
    - ใช้ปุ่มที่อยู่ใต้ข้อความแสดงสถานะเพลงเพื่อควบคุมทุกอย่าง

## 🚀 การ Deploy เพื่อรัน 24/7 (แนะนำ: Railway)

1.  **เตรียมโปรเจกต์ของคุณบน GitHub:**
    - สร้าง Repository ใหม่บน GitHub
    - อัปโหลดไฟล์ทั้งหมดในโปรเจกต์นี้ขึ้นไป (ยกเว้น `node_modules` และ `.env`)

2.  **สมัครและเข้าสู่ระบบ [Railway](https://railway.app/)**

3.  **สร้างโปรเจกต์ใหม่:**
    - คลิก "New Project" และเลือก "Deploy from GitHub repo"
    - เลือก Repository ของบอทที่คุณสร้างไว้

4.  **ตั้งค่า Environment Variables:**
    - ไปที่หน้าโปรเจกต์ของคุณใน Railway แล้วเลือกแท็บ "Variables"
    - เพิ่ม Variable ใหม่ชื่อ `DISCORD_TOKEN` และใส่ค่า Token ของบอทคุณ

5.  **Deploy:**
    - Railway จะตรวจจับ `package.json` และ `index.js` แล้วเริ่ม build และ deploy โปรเจกต์โดยอัตโนมัติ
    - เมื่อ deploy เสร็จ บอทของคุณจะออนไลน์ 24/7

## ⚠️ **คำเตือนเรื่อง Token**

**ห้ามเปิดเผย Bot Token ของคุณเด็ดขาด!** หาก Token รั่วไหล คนอื่นจะสามารถควบคุมบอทของคุณได้ ให้เก็บไว้ในไฟล์ `.env` และอย่า commit ไฟล์นี้ขึ้น GitHub เด็ดขาด
